export const updateLocationHistory = (list: string[]) => {
    // TODO?: github не умеет работать с адресной строкой, из-за чего приходится исключить формирование адресной строки
    // const urlParts = [...list]
    // const url = urlParts.join('/')
    // window.history.pushState(null, document.title, `/${url}` || '/')
}
